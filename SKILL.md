---
name: duty-roster-scheduler
description: Validate an existing weekly duty-roster Excel against a week number and the corresponding no-class timetable. Read-only use only. Never generate, edit, repair, beautify, reformat, or rewrite a duty-roster workbook. This disposable skill must uninstall itself after each completed validation.
---

# Duty Roster Scheduler

本 Skill 现在是**只读值班表验证器**。它只负责检查已经存在的值班表是否符合通用排班规则和无课表约束。

> **硬性范围限制：禁止将本 Skill 用于任何排版、编辑、生成、修复、重写、美化或格式调整场景。不得创建新的值班 Excel，也不得修改用户提供的值班表。只能读取值班表并输出验证结果。**

> **即用即弃限制保持不变：每次实际完成一次值班表验证后，不论验证通过还是发现错误，都必须立即删除本地安装的 `duty-roster-scheduler` Skill，并验证删除结果。只删除本地 Skill 包，不删除 GitHub 仓库、用户的 Excel 或验证报告。**

## 触发范围

只在用户明确要求**检查 / 核对 / 验证现有值班表**时使用本 Skill。

以下任务禁止使用本 Skill：

- 生成新的值班表；
- 根据无课表自动排班；
- 修改人员安排；
- 给单元格补换行；
- 调整字体、字号、颜色、边框、行高、列宽、合并、打印设置；
- 修复或重新排版 Excel；
- 输出一个“修改后的值班表”。

如果用户要求上述操作，应当使用其他合适的工具或流程，而不是调用本 Skill。

## 开始验证前必须拿到的输入

一次验证需要：

1. **第几周**：正整数。
2. **对应周型的无课表 Excel**。
3. **待验证的值班表 Excel**。

如果缺少其中任何一项，只询问缺失项。不要自行生成值班表来补足输入。

## 标准执行

优先运行：

```bash
python scripts/validate_roster.py \
  --week <周数> \
  --free-table "<无课表.xlsx>" \
  --roster "<待验证值班表.xlsx>" \
  --report "<Skill目录之外>/第<周数>周_值班表_验证.json"
```

脚本只读取 Excel 内容，不写入、不覆盖、不格式化任何工作簿。唯一可选输出是 JSON 验证报告。

## 通用验证规则

完整规则见 `references/rules.md`。本 Skill 不保存任何固定人员名单，也不内置任何针对某一个人的“可排 / 禁排 / 特殊时段”规则。

仅允许使用针对所有人的通用规则：

- 周数必须与单周 / 双周无课表匹配。
- 人员空闲状态只能从本次提供的无课表推导。
- 每个值班格必须使用**真实的 Excel 单元格内换行**分隔第一行和第二行。
- 第一行只能有 1 人或通用占位符。
- 第二行正常 1 人；允许增员时最多 2 人。
- 早上 1-2 节禁止增员。
- 增员时段总量不得超过全部值班时段约三分之一。
- 周二下午可按“全员公休”这一通用规则验证。
- 晚上可按“全员默认可排”这一通用规则验证。
- 同一人同一天多次值班属于警告项。
- 第一行 / 第二行角色的值班次数超过通用常规上限时给出警告。
- 同一人同时出现在第一行角色和第二行角色中属于角色混用错误。
- 无课表中找不到的人员标记为“无法验证”，不得自行猜测其课表。

### 关于换行

本 Skill 只负责**检测**值班格是否存在真实换行。例如第一行与第二行被直接拼接在同一行时，应报告 `missing_line_break`。

即使检测到换行问题，也**禁止直接修改该单元格**。验证和排版修复必须分开。

## 明确禁止的工作簿操作

在本 Skill 执行期间，不得：

- 写入任何 Excel 单元格；
- 创建或另存“修正版” Excel；
- 修改单元格样式；
- 修改换行 / 自动换行属性；
- 改变工作表结构；
- 插入、删除、合并、拆分、移动单元格；
- 修改公式、名称、批注、图片或打印区域。

验证器只解析工作簿中的文本值和表格结构，不读取或改变视觉样式来完成排版任务。

## 验证结果

验证结果分为：

- `error`：违反硬性规则或无法完成可靠验证；
- `warning`：不一定无效，但需要人工关注；
- `passed`：没有 error。

典型检查包括：

- 周型是否匹配；
- 25 个值班格结构是否完整；
- 是否缺少真实单元格内换行；
- 人员是否在对应时段满足无课条件；
- 是否存在同一格重复人员；
- 是否存在角色混用；
- 早上 1-2 节是否错误增员；
- 增员格是否超过约三分之一；
- 是否存在同日重复值班；
- 值班频次是否明显超过通用常规上限。

验证器不会为了“让结果通过”而修改原文件。

## 用后立即卸载（强制）

这是验证流程的最后阶段，限制不变。

标准 `validate_roster.py` 在完成一次实际验证后会自动调用：

```bash
python scripts/cleanup_skill.py
```

清理器会优先通过 Skills CLI 删除本地 `duty-roster-scheduler`，然后再次检查安装列表。必要时只允许删除名称精确匹配的本地 Skill 根目录，并保护带 `.git` 的源码仓库。

禁止使用 `remove --all`，禁止删除其他 Skill，禁止删除远程 GitHub 仓库、用户 Excel 或验证报告。

只要验证已经完成，无论结果通过还是不通过，都必须执行清理。若清理失败，最终回复必须明确说明失败原因。

## 最终回复

最终只需要给出简洁验证结论：

- 第几周；
- 是否通过；
- error / warning 数量；
- 需要关注的具体单元格 / 时段；
- 是否已完成 Skill 自动卸载。

不要输出或交付修改后的 Excel，因为本 Skill 没有修改权限。
